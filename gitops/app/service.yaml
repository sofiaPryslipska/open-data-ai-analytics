apiVersion: v1
kind: Service
metadata:
  name: analytics-web-service
  namespace: ai-analytics-app
  labels:
    app: analytics-web
spec:
  type: NodePort
  selector:
    app: analytics-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30050