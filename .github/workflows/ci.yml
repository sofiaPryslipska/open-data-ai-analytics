name: CI Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:
    inputs:
      module:
        description: 'Choose module to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - load_data
          - quality_analysis
          - data_research
          - visualize

jobs:
  run-modules:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: [load_data, quality_analysis, data_research, visualize]

    if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.module == 'all' || github.event.inputs.module == matrix.module }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Create artifact directory
        run: mkdir -p artifacts/${{ matrix.module }}

      - name: Run module
        env:
          PYTHONPATH: src
        run: python src/${{ matrix.module }}.py > artifacts/${{ matrix.module }}/run.log 2>&1 || true

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: result-${{ matrix.module }}
          path: artifacts/${{ matrix.module }}