# Оплати медичним закладам за програмою медичних гарантій (2024)

**Мета:** Проаналізувати, як держава фінансувала медичні заклади у 2024 році, дослідити різницю у виплатах між закладами та розподіл коштів у часі.
**Джерело даних:** [Портал відкритих даних (data.gov.ua)](https://data.gov.ua/dataset/25a46db9-2f15-4302-9b59-9bd761c80f46)

**Гіпотези для аналізу:**
1. Нерівномірність виплат: Більшість коштів отримує мала кількість великих установ.
2. Типи власності: Виплати ФОП значно нижчі, ніж державним КНП.
3. Часова активність: Виплати мають пікові дні.
*Якість даних перевірено. Аналіз підтвердив усі висунуті гіпотези.*

---

## Контейнерна архітектура (Docker)

### 1. Структура проєкту
* `data/raw/` — директорія з вхідним файлом `payments_2024.csv`.
* `data_load/` — скрипти та Dockerfile для завантаження даних у БД.
* `quality_analysis/` — скрипти та Dockerfile для аналізу якості даних.
* `data_research/` — скрипти та Dockerfile для перевірки гіпотез.
* `visualization/` — скрипти та Dockerfile для побудови графіків.
* `web/` — Flask-додаток (код та шаблони HTML) для веб-інтерфейсу.
* `compose.yaml` — головний файл оркестрації мікросервісів.

### 2. Опис сервісів
* **db (PostgreSQL):** Єдине джерело сирих даних. Використовує том `pgdata` для збереження стану.
* **data_load:** Ініціалізує таблицю і завантажує дані з CSV у PostgreSQL. Вимикається після виконання.
* **quality_analysis**, **data_research**, **visualization:** Аналітичні сервіси. Запускаються паралельно після завантаження даних. Читають БД, обчислюють результати та зберігають звіти (txt, json) і графіки (png) у спільний Docker-том `shared_data`.
* **web:** Веб-сервер. Запускається останнім, агрегує артефакти зі спільного тому та виводить дашборд у браузер.

### 3. Перелік портів
* **5432** — База даних PostgreSQL (доступна для підключення інструментами аналізу).
* **5000** — Веб-інтерфейс Flask.

### 4. Команди запуску
Для локального розгортання всього проєкту виконайте команду в кореневій директорії:
```bash
docker compose up --build
```

# Open Data AI Analytics Deployment

Цей розділ містить інструкції з розгортання проєкту в хмарній інфраструктурі Microsoft Azure за допомогою Terraform.

## Порядок розгортання

1. **Запуск середовища:** Увійдіть до Azure Portal та відкрийте Azure Cloud Shell (Bash).
2. **Підготовка інфраструктури:** Скопіюйте вміст папки `infra/terraform/` у робочу директорію Cloud Shell.
3. **Ініціалізація та запуск:**
   - Виконайте `terraform init` для підготовки провайдера.
   - Виконайте `terraform apply -auto-approve` для створення ресурсів.
4. **Передача даних:** Оскільки датасет не зберігається в репозиторії, скопіюйте файл даних на створену VM за допомогою SCP:
   `scp data/raw/payments_2024.csv azureuser@<PUBLIC_IP>:/home/azureuser/app/data/raw/`
5. **Запуск застосунку:** Підключіться до VM через SSH та виконайте `sudo docker compose up -d` у директорії `/home/azureuser/app`.

## Перевірка результатів
Застосунок доступний за адресою: `http://<PUBLIC_IP>:5000`

## Видалення ресурсів
Після завершення роботи обов'язково виконайте команду:
`terraform destroy -auto-approve`


## Моніторинг системи (Prometheus & Grafana)

Цей розділ описує налаштування та використання стеку моніторингу, розгорнутого в межах інфраструктури проєкту.

### Як розгортається моніторинг
Система моніторингу розгортається автоматично в хмарі Azure. Віртуальна машина (VM) створюється за допомогою **Terraform**, після чого інструмент **cloud-init** виконує базове налаштування середовища (встановлення Docker, завантаження вихідного коду). Запуск усіх сервісів, включно з моніторингом, здійснюється за допомогою **Docker Compose**.

### Сервіси для збору метрик
* **Prometheus:** Головний сервіс, який опитує цілі, збирає та зберігає метрики у форматі часових рядів.
* **Node Exporter:** Збирає апаратні та системні метрики з Linux-хоста (Azure VM).
* **cAdvisor:** Збирає метрики використання ресурсів (CPU, RAM) та продуктивності ізольованих Docker-контейнерів.

### Відкриті порти
Для роботи системи та доступу до сервісів в інфраструктурі Azure налаштовано відкриття наступних портів:
* `22` — SSH-доступ до віртуальної машини.
* `3000` — Веб-інтерфейс Grafana.
* `9090` — Веб-інтерфейс Prometheus та збір метрик.
* `5000` — Доступ до основного веб-застосунку.

### Як відкрити Grafana
1. Дізнайтеся публічну IP-адресу віртуальної машини в Azure Portal.
2. Відкрийте браузер і перейдіть за адресою: `http://<PUBLIC_IP>:3000`
3. Увійдіть у систему за допомогою стандартних облікових даних (або тих, що були задані при першому вході):
   * Логін: `admin`
   * Пароль: `admin`
4. У розділі **Connections -> Data sources** підключено Prometheus за внутрішньою адресою `http://prometheus:9090`.

### Створені панелі (Дашборди)
У Grafana налаштовано візуалізацію даних з Prometheus. Наразі реалізовано наступні панелі:
1.  **Node Exporter Full (ID 1860):** Комплексний дашборд для моніторингу системних метрик віртуальної машини (завантаження CPU, використання оперативної пам'яті, стан дискової підсистеми).
2.  **Контейнерний моніторинг (Custom Panel):** Власна панель, побудована за допомогою PromQL запиту `container_memory_usage_bytes{name!=""}`, яка детально демонструє обсяг оперативної пам'яті, що споживається кожним окремим Docker-контейнером інфраструктури.